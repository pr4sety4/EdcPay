<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adi.edcpay">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity android:name=".content.Notification" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_scrolling"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PromoActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".PinActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".content.Setting" />
        <activity android:name=".FormActivity" />
        <activity android:name=".content.Scanner" />
        <activity android:name=".promoListActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".DetailTransactionActivity" />
        <activity android:name=".ChooseBuyActivity" />
        <activity android:name=".BuyActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".EditActivity" />
        <activity android:name=".content.DetailsWalletActivity" />
        <activity android:name=".WithdrawActivity" />
        <activity android:name=".AboutActivity"></activity>
    </application>

</manifest>